# Misc Results

One-off test runs, exploratory experiments, and archived external data.

| Item | Description |
|------|-------------|
| `TestFlockingRun/` | Single test run used to verify flocking setup |
| `Experiment_misc_agents/` | Exploratory runs with non-standard agent configurations |
| `run0-1.csv` | Standalone single-run metrics file |
| `pranav/` | Archived experiment data from external collaborator — contains `experiment_day*` folders (days 2–65), each with 10 runs, metrics CSVs, and PNG/PDF plots. Also includes `Agents_.zip` and `Experiment_.tar.xz` compressed archives. |
